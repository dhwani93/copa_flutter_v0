
import 'package:flutter/material.dart';

class AppColors {
  // Core Colors
  static const Color background = Color(0xFF101014);
  static const Color surface = Color(0xFF1A1A1F);
  static const Color overlay = Colors.white10;

  // Accent + Primary Colors
  static const Color accentOrange = Color(0xFFFFA726); // Soft orange
  static const Color primaryBlue = Color(0xFF42A5F5); // Google-style blue

  // Feedback
  static const Color successGreen = Color(0xFF66BB6A);
  static const Color errorRed = Color(0xFFEF5350);

  // Text
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textMuted = Colors.white54;

  // Border
  static const Color divider = Colors.white10;
}
